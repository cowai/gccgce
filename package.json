{
  "name": "gccgce",
  "version": "1.0.0",
  "description": "This GAS just changes the color of Google calendar events.",
  "license": "MIT",
  "private": true,
  "scripts": {
    "push": "clasp push",
    "lint": "eslint 'src/**/*.ts'",
    "fix": "prettier --write 'src/**/*.ts' && eslint --fix 'src/**/*.ts'"
  },
  "devDependencies": {
    "@types/google-apps-script": "^1.0.41",
    "@typescript-eslint/eslint-plugin": "^5.5.0",
    "@typescript-eslint/parser": "^5.5.0",
    "eslint": "^8.4.0",
    "eslint-config-prettier": "^8.3.0",
    "prettier": "^2.5.1",
    "typescript": "^4.5.2"
  }
}
